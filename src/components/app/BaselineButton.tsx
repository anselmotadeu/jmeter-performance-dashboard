'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';import { Flag } from 'lucide-react';export default function BaselineButton({id,active}:{id:string;active:boolean}){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);async function set(){setLoading(true);setError(null);try{const response=await fetch(`/api/runs/${id}/baseline`,{method:'PUT'});if(!response.ok)throw new Error('Falha ao definir baseline');router.refresh()}catch(err){setError(err instanceof Error?err.message:'Não foi possível definir a baseline.')}finally{setLoading(false)}}return <div>{error&&<div className="mb-2 rounded-lg border border-red-200 bg-red-50 p-2 text-sm text-red-700">{error}</div>}<button onClick={set} disabled={loading||active} className="inline-flex h-11 items-center gap-2 rounded-xl border border-indigo-200 bg-white px-4 text-sm font-black text-indigo-700 disabled:opacity-60 dark:bg-slate-900"><Flag className="h-4 w-4"/>{active?'Baseline atual':loading?'Atualizando...':'Definir baseline'}</button></div>}
