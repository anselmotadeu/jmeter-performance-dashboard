import Link from 'next/link';import { headers } from 'next/headers';import { Activity,AlertTriangle,ArrowRight,FileSearch,FolderKanban,Gauge } from 'lucide-react';import { auth } from '@/lib/auth';import { getOverview } from '@/lib/run-data';
export default async function Page(){const session=await auth.api.getSession({headers:await headers()});if(!session)return null;const{metrics,recent}=await getOverview(session.user.id);const cards=[{label:'Análises salvas',value:metrics.runs,icon:FileSearch},{label:'Requisições avaliadas',value:Number(metrics.requests).toLocaleString('pt-BR'),icon:Activity},{label:'Projetos',value:metrics.projects,icon:FolderKanban},{label:'Regressões',value:metrics.regressions,icon:AlertTriangle}];return <div className="space-y-7"><section className="relative overflow-hidden rounded-[2rem] bg-[radial-gradient(circle_at_85%_15%,rgba(34,211,238,.24),transparent_28%),linear-gradient(125deg,#312e81,#4338ca_55%,#0e7490)] p-7 text-white shadow-xl sm:p-10"><p className="text-xs font-black uppercase tracking-[.25em] text-cyan-200">Performance intelligence</p><h1 className="mt-3 text-4xl font-black">Olá, {session.user.firstName||session.user.name}.</h1><p className="mt-4 max-w-2xl leading-7 text-indigo-100">Analise JMeter e k6, acompanhe baselines e identifique regressões antes que cheguem à produção.</p><div className="mt-7 flex flex-wrap gap-3"><Link href="/analisar" className="inline-flex items-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-black text-indigo-900">Nova análise <ArrowRight className="h-4 w-4"/></Link><Link href="/resultados" className="rounded-xl border border-white/30 bg-white/10 px-5 py-3 text-sm font-black">Ver histórico</Link></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({label,value,icon:Icon})=><div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600 dark:bg-indigo-950/50 dark:text-indigo-300"><Icon className="h-5 w-5"/></div><div className="mt-5 text-3xl font-black">{value}</div><div className="text-sm text-slate-500">{label}</div></div>)}</section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center justify-between"><div><h2 className="text-xl font-black">Análises recentes</h2><p className="text-sm text-slate-500">Últimos resultados por projeto.</p></div><Link href="/resultados" className="text-sm font-black text-indigo-600">Ver todas</Link></div>{recent.length?<div className="mt-5 divide-y dark:divide-slate-800">{recent.map(run=><Link key={run.id} href={`/resultados/${run.id}`} className="grid gap-2 py-4 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><div className="font-black">{run.title}</div><div className="text-xs text-slate-500">{run.framework} · {new Date(run.createdAt).toLocaleDateString('pt-BR')}</div></div><div className="text-sm font-bold">{(run.successCount+run.errorCount).toLocaleString('pt-BR')} req.</div><span className={'w-fit rounded-full px-2.5 py-1 text-xs font-black '+(run.errorCount?'bg-amber-50 text-amber-700':'bg-emerald-50 text-emerald-700')}>{run.errorCount} erros</span></Link>)}</div>:<div className="mt-6 rounded-2xl border border-dashed p-8 text-center"><Gauge className="mx-auto h-10 w-10 text-slate-400"/><p className="mt-3 font-black">Nenhuma análise salva</p><p className="text-sm text-slate-500">Use um arquivo em `Tests/` para começar.</p></div>}</section></div>}
